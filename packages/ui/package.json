{
  "name": "@proofvault/ui",
  "version": "0.0.0",
  "private": true,
  "description": "Shared UI components — shadcn/ui primitives, ProofVault brand tokens, verification card.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'TODO: build ui (added in step 6)'",
    "lint": "echo 'TODO'",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  }
}
