{
  "name": "@proofvault/contract",
  "version": "0.0.0",
  "private": true,
  "description": "ProofVault Compact smart contract — verifyAndRecord, redeem, tier ladder, nullifier registry.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'TODO: compactc compile (added in step 4)'",
    "test": "echo 'TODO: jest contract integration tests (added in step 4)'",
    "lint": "echo 'TODO'",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist managed .turbo"
  }
}
