{
  "name": "@proofvault/shared-types",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript types — AttestationPayload, ProofRecord, tier constants, network IDs.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "echo 'TODO'",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  }
}
